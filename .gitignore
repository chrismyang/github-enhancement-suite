# Playwright MCP session artifacts (snapshots, console logs)
.playwright-mcp/

# Dependencies (none currently; tests use Node's built-in runner)
node_modules/

# Build output (store-ready zips)
dist/

# OS / editor cruft
.DS_Store
*.swp
